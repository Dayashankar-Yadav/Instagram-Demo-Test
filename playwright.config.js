// playwright.config.js

module.exports = {
  use: {
    browserName: 'chromium',
    headless: false, // Set headless to false to launch browser with UI
    // Optionally, specify executablePath if needed
  },
};
